name: Reviewpad Action Team A
on:
  pull_request:
    paths:
      - 'team-a/**'

jobs:
  reviewpad-action-team-a:
    runs-on: ubuntu-latest
    steps:
      - name: Running reviewpad action
        uses: reviewpad/action@v3.x
        with:
          file: '.reviewpad/team-a.yml'
